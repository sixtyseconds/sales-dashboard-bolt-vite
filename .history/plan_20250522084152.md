# Plan: Refactor Cohort Tracking HTML to React and Add New Feature

## Phase 1: Project Setup & Initial Refactoring

1.  [x] **Clarify Project Setup:**
    *   Yes, a Vite project is in place.
    *   ~~If not, what is the preferred way to set one up (e.g., Create React App, Vite)?~~
    *   ~~What styling solution is preferred (e.g., Tailwind CSS, CSS Modules, Styled Components)? The HTML uses Tailwind.~~ Existing project uses Vite and Tailwind.
2.  [x] **Clarify "New Feature":**
    *   ~~What is the new feature that needs to be built and added to the navigation as "Clients"?~~ The "Clients" feature will be the React version of the `cohort-tracking.html` page.
3.  [x] **Set up React Project (if needed):** Based on user preference. (Vite project already exists)
4.  [x] **Install Dependencies:** `chart.js`, `react-chartjs-2`, potentially `react-router-dom`. (`react-router-dom` was already installed, `chart.js` and `react-chartjs-2` are now installed.)
5.  [ ] **Create Main `App` Component / `DashboardPage` Component:** This will be the entry point for the refactored dashboard.
6.  [ ] **Create Basic Layout Component:** Replicate the main structure from the HTML (`<div class="relative flex size-full min-h-screen flex-col ...">` etc.).

## Phase 2: Component Creation (Based on `cohort-tracking.html`)

*   For each component, we will:
    *   Create a new `.jsx` or `.tsx` file.
    *   Translate the HTML structure to JSX.
    *   Migrate relevant JavaScript logic (e.g., Chart.js initialization) into the component using React hooks (`useState`, `useEffect`).
    *   Style the component (likely using Tailwind CSS, as in the original).

1.  [ ] **Component: `StatsCards`**
    *   Section: "Total Active Clients", "Monthly Recurring Revenue", "Monthly Churn Rate", "Clients Signed This Month".
2.  [ ] **Component: `LifecycleStageCards`**
    *   Section: "Client Lifecycle Stages" (Onboarding, Active, Given Notice, Cancelled).
3.  [ ] **Component: `RevenueTrendChart`**
    *   Chart: "Revenue Trend (Last 12 Months)".
4.  [ ] **Component: `RevenueBreakdownCharts`**
    *   Charts: "One-Off vs. Subscription", "Subscription Breakdown".
5.  [ ] **Component: `ClientViews`**
    *   Includes: View toggle buttons (Table/Kanban), filter dropdowns.
    *   Sub-component: `ClientTable`
    *   Sub-component: `ClientKanbanBoard`
6.  [ ] **Component: `RetainedBillingsChart`**
    *   Chart: "Retained Billings Forecast".
7.  [ ] **Component: `ChurnAnalysisSection`**
    *   Sub-component: `ChurnRateTrendChart`
    *   Sub-component: `ChurnReasonsChart`
    *   Sub-component: `CohortAnalysisTable`

## Phase 3: Navigation and New Feature Integration

1.  [ ] **Set up Navigation/Routing:**
    *   Integrate `react-router-dom` if a multi-page feel is desired or if "Clients" is a distinct page.
    *   Add a "Clients" link to the main navigation structure.
2.  [x] **Develop New "Clients" Feature:**
    *   ~~(Details to be provided by the user)~~ The feature is the refactored `cohort-tracking.html` page.
3.  [ ] **Integrate New Feature:** Connect the "Clients" feature to the navigation and ensure it displays correctly.

## Phase 4: Review and Refinement

1.  [ ] **Code Review:** Check for consistency, best practices, and potential improvements.
2.  [ ] **Testing (Optional but Recommended):** Add basic unit or integration tests.
3.  [ ] **Final Adjustments:** Based on review and testing. 